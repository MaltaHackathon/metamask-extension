# New MetaMask Browser Extension which is fork of the original metamask
The most important and essential feature which we have implemented is a possibility to switch MetaMask in auto-confirmation mode.
